# Wix Headless SDK — visitor token (public, client-side safe)
NEXT_PUBLIC_WIX_CLIENT_ID=your-wix-headless-oauth-client-id

# Wix Backend API key (server-only — never expose to client)
WIX_BACKEND_KEY=your-wix-backend-api-key
